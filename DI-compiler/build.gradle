apply plugin: 'java-library'

dependencies {
    compile deps.java.poet
    compile deps.auto.service
}

compileJava {
    sourceCompatibility = versions.java
    targetCompatibility = versions.java
}