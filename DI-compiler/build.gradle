apply plugin: 'java'

dependencies {
    compile project(deps.ys.annotation)
    compile deps.square.poet
    compile deps.google.auto.service
    compile 'org.projectlombok:lombok.ast:0.2'
}

compileJava {
    sourceCompatibility = versions.java
    targetCompatibility = versions.java
}